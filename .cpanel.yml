---
deployment:
  tasks:
    - export DEPLOYPATH=/home/fallphyl/public_html/
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp .htaccess $DEPLOYPATH 2>/dev/null || :
    - /bin/cp .cpanel.yml $DEPLOYPATH 2>/dev/null || :
